spec:
  name: albyhub
  services:
    - name: web
      git:
        branch: master
        repo_clone_url: https://github.com/getAlby/nostr-wallet-connect-next.git
      instance_size_slug: basic-xxs
      envs:
      - key: LDK_ESPLORA_SERVER
        value: "https://electrs.albylabs.com"
      - key: LOG_EVENTS
        value: "true"
